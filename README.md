# Git: Основы ветвления и слияния

Этот репозиторий создан для конспектирования ключевых концепций работы с Git, изученных в мини-лекции.

## 📚 Изученные концепции

### 1. Ветки (Branches)
- **Назначение:** Изолированная разработка фич, исправлений или экспериментов без воздействия на стабильную основную ветку (`main`/`master`).
- **Основные команды:**
  - `git branch <имя>` — создать ветку.
  - `git checkout <имя>` / `git switch <имя>` — переключиться на ветку.
- **Аналог:** Работа с черновиком документа, не затрагивая оригинал.

### 2. Коммиты (Commits)
- **Назначение:** Фиксация снимка изменений в истории проекта.
- **Правило:** Частые коммиты с ясными сообщениями, описывающими *что* сделано, а не просто "правки".
- **Команда:** `git commit -m "Сообщение"`.

### 3. Слияние изменений: Merge vs Rebase
- **Merge (`git merge`):**
  - Объединяет ветки через **коммит слияния**.
  - **Плюс:** Сохраняет полную историю.
  - **Минус:** Может создать сложную нелинейную историю.
- **Rebase (`git rebase`):**
  - «Перемещает» коммиты одной ветки на верхушку другой, переписывая историю.
  - **Плюс:** Создаёт линейную, чистую историю.
  - **Опасность:** **Запрещено для публичных коммитов**, так как ломает историю у коллег.
- **Вывод:** Используйте `merge` для публичной истории, `rebase` — для поддержания чистоты локальной ветки.

### 4. Точечный перенос: Cherry-pick
- **Назначение:** Перенос отдельного коммита из одной ветки в другую.
- **Команда:** `git cherry-pick <хэш_коммита>`.
- **Применение:** Для срочных исправлений или переноса упущенных изменений. Не злоупотреблять.

### 5. Навигация и откаты
- **`git checkout`:** Устаревшая, но многофункциональная команда:
  1. Переключение на ветку или коммит.
  2. Откат файла к состоянию последнего коммита (`git checkout -- <файл>`).
- **Современные альтернативы:**
  - `git switch <ветка>` — для переключения.
  - `git restore <файл>` — для отката изменений в файле.

## 🚀 Типовой рабочий процесс (Feature Branch Workflow)
1.  `git switch main && git pull` — начать с актуальной основы.
2.  `git switch -c feature/name` — создать ветку для задачи.
3.  Работа: вносить изменения, делать `git add` и `git commit`.
4.  Периодически: `git rebase main` — подтягивать обновления в свою ветку и решать конфликты.
5.  Завершение:
    - `git switch main && git pull`
    - `git merge feature/name --no-ff` (или `git rebase main` + `git merge --ff-only`)
6.  `git push origin main`
7.  `git branch -d feature/name` — удалить локальную ветку.

## 🧠 Ключевое правило
**Ветка `main` должна всегда оставаться в стабильном, рабочем состоянии.** Вся разработка ведётся в отдельных ветках.
